#version 330 core
in vec3 vertex;
//layout (location = 1) in vec3 colorVec;
uniform mat4 projection;
uniform mat4 model;

out vec3 fragColor;

void main()
{

    gl_Position = projection *vec4(vertex, 1.0);
}
